name: Deploy to Cloud Run

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      target:
        description: Which service to deploy
        type: choice
        options: [both, backend, dashboard]
        default: both

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      dashboard: ${{ steps.filter.outputs.dashboard }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'src/**'
              - 'package.json'
              - 'package-lock.json'
              - 'tsconfig.json'
              - 'Dockerfile'
              - 'deploy/cloudrun/deploy-backend.sh'
              - '.github/workflows/deploy.yml'
            dashboard:
              - 'dashboard/**'
              - 'deploy/cloudrun/deploy-dashboard.sh'
              - '.github/workflows/deploy.yml'

  backend:
    needs: changes
    if: |
      (github.event_name == 'push') ||
      (github.event_name == 'workflow_dispatch' && (inputs.target == 'both' || inputs.target == 'backend'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: production
    steps:
      - uses: actions/checkout@v4
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOYER_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Deploy backend
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          REGION: ${{ vars.GCP_REGION }}
          AR_REPO: ${{ vars.AR_REPO }}
          BACKEND_SERVICE_ACCOUNT: ${{ secrets.BACKEND_SERVICE_ACCOUNT }}
          IMAGE_TAG: ${{ github.sha }}
          PUBLIC_BASE_URL: ${{ vars.PUBLIC_BASE_URL }}
          FRONTEND_URL: ${{ vars.FRONTEND_URL }}
          MCP_URL: ${{ vars.MCP_URL }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          INTERNAL_API_SECRET: ${{ vars.INTERNAL_API_SECRET_ID }}
          OPENAI_API_KEY: ${{ vars.OPENAI_API_KEY_ID }}
          JWT_SECRET: ${{ vars.JWT_SECRET_ID }}
          MEMORY_MASTER_KEY: ${{ vars.MEMORY_MASTER_KEY_ID }}
          GOOGLE_CLIENT_ID: ${{ vars.GOOGLE_CLIENT_ID }}
          GOOGLE_REDIRECT_URI: ${{ vars.GOOGLE_REDIRECT_URI }}
          TALLEI_BILLING__LEMONSQUEEZY_API_KEY: ${{ secrets.TALLEI_BILLING__LEMONSQUEEZY_API_KEY || secrets.LEMONSQUEEZY_API_KEY }}
          TALLEI_BILLING__LEMONSQUEEZY_WEBHOOK_SECRET: ${{ secrets.TALLEI_BILLING__LEMONSQUEEZY_WEBHOOK_SECRET || secrets.LEMONSQUEEZY_WEBHOOK_SECRET }}
          TALLEI_BILLING__LEMONSQUEEZY_PRO_VARIANT_ID: ${{ vars.TALLEI_BILLING__LEMONSQUEEZY_PRO_VARIANT_ID || vars.LEMONSQUEEZY_PRO_VARIANT_ID }}
          TALLEI_BILLING__LEMONSQUEEZY_POWER_VARIANT_ID: ${{ vars.TALLEI_BILLING__LEMONSQUEEZY_POWER_VARIANT_ID || vars.LEMONSQUEEZY_POWER_VARIANT_ID }}
          TALLEI_AUTH__CONTINUATION_PRIVATE_KEY: ${{ secrets.TALLEI_AUTH__CONTINUATION_PRIVATE_KEY }}
          TALLEI_AUTH__CONTINUATION_PUBLIC_KEY: ${{ secrets.TALLEI_AUTH__CONTINUATION_PUBLIC_KEY }}
          TALLEI_AUTH__API_KEY_PEPPER: ${{ secrets.TALLEI_AUTH__API_KEY_PEPPER || secrets.API_KEY_PEPPER }}
          UPLOADTHING_TOKEN: ${{ secrets.UPLOADTHING_TOKEN }}
          TALLEI_SIGNUP__EMAIL_FROM_NAME: ${{ vars.TALLEI_SIGNUP__EMAIL_FROM_NAME }}
          TALLEI_SIGNUP__EMAIL_FROM_EMAIL: ${{ vars.TALLEI_SIGNUP__EMAIL_FROM_EMAIL }}
          TALLEI_SIGNUP__EMAIL_REPLY_TO: ${{ vars.TALLEI_SIGNUP__EMAIL_REPLY_TO }}
          TALLEI_SIGNUP__RESEND_API_KEY: ${{ secrets.TALLEI_SIGNUP__RESEND_API_KEY }}
          TALLEI_SIGNUP__SLACK_WEBHOOK_URL: ${{ secrets.TALLEI_SIGNUP__SLACK_WEBHOOK_URL }}
        run: bash deploy/cloudrun/deploy-backend.sh

  dashboard:
    needs: changes
    if: |
      (github.event_name == 'push' && needs.changes.outputs.dashboard == 'true') ||
      (github.event_name == 'workflow_dispatch' && (inputs.target == 'both' || inputs.target == 'dashboard'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: production
    steps:
      - uses: actions/checkout@v4
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOYER_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Deploy dashboard
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          REGION: ${{ vars.GCP_REGION }}
          AR_REPO: ${{ vars.AR_REPO }}
          DASHBOARD_SERVICE_ACCOUNT: ${{ secrets.DASHBOARD_SERVICE_ACCOUNT }}
          IMAGE_TAG: ${{ github.sha }}
          NEXTAUTH_URL: ${{ vars.NEXTAUTH_URL }}
          NEXT_PUBLIC_APP_URL: ${{ vars.NEXT_PUBLIC_APP_URL }}
          BACKEND_URL: ${{ vars.BACKEND_URL }}
          API_PROXY_TARGET: ${{ vars.API_PROXY_TARGET }}
          NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN: ${{ vars.NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN || vars.NEXT_PUBLIC_POSTHOG_KEY }}
          NEXT_PUBLIC_POSTHOG_HOST: ${{ vars.NEXT_PUBLIC_POSTHOG_HOST }}
          GOOGLE_CLIENT_ID: ${{ vars.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ vars.GOOGLE_CLIENT_SECRET_ID }}
          INTERNAL_API_SECRET: ${{ vars.INTERNAL_API_SECRET_ID }}
          NEXTAUTH_SECRET: ${{ vars.NEXTAUTH_SECRET_ID }}
          UPLOADTHING_TOKEN: ${{ secrets.UPLOADTHING_TOKEN }}
        run: bash deploy/cloudrun/deploy-dashboard.sh
